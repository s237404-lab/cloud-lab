
services:
  backend:
    image: nhuy237404/mern-backend:1.0
    ports:
      - "5000:5000"
    environment:
      MONGODB_URI: ${MONGODB_URI}
      PORT: "5000"

  frontend:
    image: nhuy237404/mern-frontend:1.0
    ports:
      - "3000:3000"
    depends_on:
      - backend
